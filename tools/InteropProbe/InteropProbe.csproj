<Project Sdk="Microsoft.NET.Sdk">

  <!-- Standalone diagnostic. Deliberately not listed in VintageStory.slnx:
       it is a throwaway capability probe, not part of the client build. -->

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>Optimum.InteropProbe</RootNamespace>
    <AssemblyName>InteropProbe</AssemblyName>
    <InvariantGlobalization>true</InvariantGlobalization>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="OpenTK" Version="4.9.4" />
  </ItemGroup>

</Project>
